spring:
  application:
    name: server-voucher-core-${api.config.version}
  servlet:
    multipart:
      enabled: true
      max-file-size: 20MB
      max-request-size: 200MB
#server
server:
  port: 8061

api:
  config:
    version: v1