spring:
  application:
    name: server-voucher-oss-${api.config.version}
  servlet:
    multipart:
      enabled: true
      max-file-size: 20MB
      max-request-size: 200MB
#server
server:
  port: 8062

api:
  config:
    version: v1